# Terraform state files (sensitive)
*.tfstate
*.tfstate.backup

# Terraform crash logs
crash.log

# Terraform directories
.terraform/
.terraform.lock.hcl

# AWS credentials or key files (never commit private keys!)
*.pem
*.key
*.pub

# SSH keys (your private/public keys)
terraform/.ssh/*

# Any local override or environment files
terraform.tfvars
*.auto.tfvars
